name: Build desktop bundles

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-22.04]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev \
            libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build and validate browser extension
        run: |
          npm run build:extension
          node --check extension/web-app.js
          node --check extension/content.js
          node --check extension/background.js
          node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('extension/manifest.json')); if(m.manifest_version!==3) process.exit(1)"
          npm test

      - name: Build Tauri bundles
        run: npm run tauri build

      - name: Upload bundles
        uses: actions/upload-artifact@v4
        with:
          name: mineparser-${{ runner.os }}
          path: src-tauri/target/release/bundle/

      - name: Upload browser extension
        uses: actions/upload-artifact@v4
        with:
          name: mineparser-extension-${{ runner.os }}
          path: extension/
